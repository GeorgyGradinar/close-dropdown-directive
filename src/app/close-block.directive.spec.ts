import { CloseBlockDirective } from './close-block.directive';

describe('CloseBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseBlockDirective();
    expect(directive).toBeTruthy();
  });
});
